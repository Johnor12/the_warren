# boards — maintainer notes

- `test-board.ts` — `TestCard` (41x63mm, white faces with "Front"/"Back"
  text) and a `main()` that places it at the center of a 500x500mm board,
  prints the board summary, and exports the face images to `out/`.
