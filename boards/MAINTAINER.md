# boards — maintainer notes

- `test-board.ts` — `TestCard` (41x63mm, white faces with "Front"/"Back"
  text) and `buildBoard()`, which places it at the center of a 500x500mm
  board. Run directly, it also prints the board summary and exports the
  face images to `out/`.
