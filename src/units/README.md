# units

Translates between physical dimensions (millimeters, the canonical unit for
game components) and pixels (the unit for images and rendering).

## API

- `PX_PER_MM` — the system-wide rendering resolution.
- `mmToPx(mm)` — convert millimeters to whole pixels.
- `pxToMm(px)` — convert pixels to millimeters.
